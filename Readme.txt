A SPANET spa heater being used as a pool heater.

The SPANET expects its own controller. We are using a STM32 custom board as the controller.
Information about current electricity price etc are provided by an MQTT server.

